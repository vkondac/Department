spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3307/departments?useSSL=false
    driverClassName: com.mysql.cj.jdbc.Driver
    password: Andjele1!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'

logging:
  level:
    root: debug

